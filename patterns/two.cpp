#include <iostream>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int row = n;
    while (n > 0)
    {
        /* code */

        while (row > 0)
        {
        }
    }

    return 0;
}